item_name = input("Enter Item name = ")

quantity = int(input("Enter quantity = "))

per_price = float(input("Enter per item price = "))

total = quantity * per_price

print(f"Total cost of {item_name} = {total}")








